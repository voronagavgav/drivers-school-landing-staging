# Task: <name>

**Status:** active   <!-- active | blocked | done -->
<!-- **Driver:** auto      uncomment to let `driver.sh run-all` drive this task autonomously in the backlog -->
<!-- **Evaluate:** yes     uncomment to force an independent evaluator judge at done-claim (high-stakes tasks) -->
**Updated:** <UTC>
**Last compute:** <laptop | mac-mini | sandbox | cloud-agent>

## Goal
<What "done" looks like — a numbered list of BOOLEAN pass/fail acceptance criteria an agent can
definitively check (e.g. "Run npm test - exits 0", "File X contains Y", "GET /health returns 200").
Avoid subjective criteria ("works correctly", "looks good"). Mechanically-checkable → add a verify.sh.>

## Constraints / decisions
- <decision> — <why>

## Plan
<!-- Optional: for complex tasks, decompose into ordered steps so a fresh-context tick executes
     against a roadmap instead of re-deriving "what's next" each time. Skip for simple/linear tasks. -->
- [ ] <step 1>

## Done
- [ ] <completed step>

## Next
- [ ] <the single immediate next action>

## Artifacts
- <path-in-Mesa> — <what it is>

## Log
- <UTC> <compute>: <what happened this session>
