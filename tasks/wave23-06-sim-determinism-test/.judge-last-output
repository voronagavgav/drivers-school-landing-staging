/Users/clpc/mesa/bin/driver.sh: line 194: describe.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: it.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: ctx.skip: command not found
All acceptance criteria verified against reality:

1. **Compact cell + shared code path** ✓ — `lib/exam-allocator-sim.determinism.test.ts` imports `runCell`/`CellParams` from `@/lib/exam-allocator-sim` (the wave23-05 pure engine) and runs a single compact cell `{prior:"median", horizonDays:14, budget:15, replicas:5}` at `SEED=42`. It re-implements nothing — same `runCell` the tsx runner uses.

2. **Frozen literals ≥6 sig-digits + reproducibility-pin header** ✓ — six 16-digit literals (`BASELINE_MEAN`, `ALLOCATOR_MEAN`, `LIFT_MEAN`, `LIFT_SE`, CI bounds) asserted via `toBe`. The header (lines 5–18) explicitly states the numbers were captured from `runCell(CELL, 42)` and are a REPRODUCIBILITY pin, "NOT a correctness oracle."

3. **`npm test` green + collected** ✓ — full unit suite: 76 files / 799 tests pass, exit 0; `npx vitest list` collects `exam-allocator-sim.determinism` (grep count = 2).

4. **Typecheck green** ✓ — `npm run -s typecheck` exits 0.

5. **Pure unit, no runner/fs import** ✓ — imports only `vitest` and `@/lib/exam-allocator-sim`; no `scripts/spikes`, no `node:fs`, no DB/`server-only`. Ran under default (non-integration) vitest config with no DB.

**Structural-trap check:** The self-consistency (frozen literal = whatever seed-42 yields) is not a disqualifying self-referential-oracle case. This task is *explicitly* a reproducibility pin, not a correctness oracle — the Goal itself mandates that framing, and the actual correctness oracle exists externally (wave23-01 python + wave23-03 allocator oracle). Trap (e) fixture-dodging is inapplicable: there is no spec-pinned *directional* claim here; the test pins byte-exact determinism, which is exactly what the spec's Deliverable 3 (second half) requires. The passing `toBe` assertions independently prove the frozen literals reproduce the real seed-42 output.

VERDICT: ACCEPT — all acceptance criteria are objectively met.
