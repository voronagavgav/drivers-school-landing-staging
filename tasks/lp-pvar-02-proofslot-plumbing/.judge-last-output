/Users/clpc/mesa/bin/driver.sh: line 194: describe.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: it.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: ctx.skip: command not found
All 8 acceptance criteria verified independently against the actual files:

| # | Criterion | Result |
|---|-----------|--------|
| 1 | `proofSlot?: React.ReactNode` added to props type + destructure | ✅ both present in diff (`_body.tsx:616,625`) |
| 2 | Slot branches on `proofSlot === undefined` (not truthy short-circuit); undefined→band, null→nothing, node→node | ✅ `proofSlot === undefined ? (<band/>) : proofSlot` |
| 3 | `page.tsx` zero-diff | ✅ `git diff --stat` empty |
| 4 | `_hero-prospekt.tsx` zero-diff | ✅ `--exit-code` clean |
| 5 | Additive-only; `.v36 .proof{`, `.proof-card{`, `.proof-say{`, `.proof-num{` present verbatim; default band markup unchanged | ✅ diff touches only the JSX region (re-indented, byte-identical markup); no STYLES rule modified |
| 6 | `npm run -s typecheck` exit 0 | ✅ exit 0 |
| 7 | Canonical `/lp/v36` `.proof` renders "1 757"/"986"/"45"/«Офіційний банк питань» | ✅ browser check ran and passed ("canonical proof band intact"); figures sourced from `copy.ts` statement, rendered by the `undefined` default branch |
| 8 | `git status` scoped to `app/lp/v36/` + task dir | ✅ only `_body.tsx`, `journal.md`, `PLUMBING-VERIFY.txt` |

Structural trap check: no test-oracle involved (pure plumbing task); verify greps are legit and anchored on real tokens; the default band's DOM is preserved (rendered content unchanged when `proofSlot === undefined`); the `undefined`/`null` distinction is correctly implemented, not a `proofSlot && …` truthy short-circuit that would drop the band on the canonical route. The browser criterion was actually exercised (not merely SKIP-deferred) and passed on the served origin. I re-ran `verify.sh` myself → `OK lp-pvar-02`, exit 0.

VERDICT: ACCEPT
