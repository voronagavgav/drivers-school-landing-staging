# Task: wave23-06-sim-determinism-test

**Status:** done
**Driver:** auto
**Updated:** 2026-07-14
**Last compute:** mac-mini

**Artifacts:** `lib/exam-allocator-sim.determinism.test.ts` (compact one-cell determinism pin, seed 42).

IMPLEMENTATION of the reproducibility pin (spec Deliverable 3, second half): "a COMPACT version of one
cell ships as a unit test pinning the sim's determinism byte-exactly, seed 42, so the reported numbers
are reproducible by anyone." Depends on: wave23-05.

This test does NOT re-verify the allocator's CORRECTNESS (that is the wave23-01→04 oracle chain) — it
pins REPRODUCIBILITY: the same seed → the same numbers, so the report's figures can be regenerated by
anyone. Self-consistency is the correct discipline here (the frozen number IS whatever seed-42 yields).

## Goal
PASS = ALL true:

1. `lib/exam-allocator-sim.determinism.test.ts` exists and runs a COMPACT single cell of the sim (one
   profile × one short horizon × one budget × a small replica count) with `seed = 42` by importing the
   shared `runCell` from `@/lib/exam-allocator-sim` (the pure engine wave23-05 exports) — the test must
   NOT re-implement the sim, and the tsx runner + this test share ONE code path.
2. The test asserts the cell's output (e.g. allocator mean pass-prob, baseline mean pass-prob, lift) to
   a FROZEN literal (≥ 6 significant digits) captured from the actual seed-42 run at authoring time. A
   header comment states the numbers were captured from `runCell(seed=42, ...)` and are a
   REPRODUCIBILITY pin, not a correctness oracle.
3. `npm test` exits 0 (the test is part of the unit suite); `npx vitest list` collects
   `exam-allocator-sim.determinism` (var-captured, herestring token-retry).
4. `npm run -s typecheck` exits 0.
5. The unit test imports `runCell` from `@/lib/exam-allocator-sim` and does NOT import the
   `scripts/spikes/` tsx runner or any Node-fs entry-point code (keeps it collectable in the pure unit
   suite; no DB, no `server-only`).

## Constraints / decisions
- Refactor, don't fork: the tsx script (wave23-05) and this test call the SAME exported sim cell fn, so
  a divergence between "what the report ran" and "what the test pins" is impossible.
- Keep the cell SMALL (fast unit test): a handful of replicas / a short horizon is enough to pin
  determinism; the full grid stays in the tsx script.
- This is a self-consistency pin BY DESIGN (reproducibility, not correctness) — no external oracle
  needed; the correctness oracle is the python (wave23-01) + the allocator oracle test (wave23-03).

## Acceptance
verify.sh: file collected + green in `npm test`; typecheck green; asserts against a frozen literal
(grep for a `toBe`/`toBeCloseTo` on the pinned number).

## Next
- [x] Export `runCell` from the sim module; author the compact seed-42 determinism test with frozen
      literals captured from the real run.
- (none — Goal met; verify.sh green.)

## Log
- 2026-07-14 mac-mini: planned.
- 2026-07-14 ClPcs-Mac-mini: `runCell` was already exported by wave23-05, so no sim-module change needed.
  Authored `lib/exam-allocator-sim.determinism.test.ts` — compact cell {median, h=14, B=15, 5 replicas},
  seed 42, importing `runCell` via the `@/lib/exam-allocator-sim` alias (gate requires the alias literal).
  Captured frozen literals from the real `npx tsx` run (base=0.5702272394905409, alloc=0.5663572662013813,
  lift=-0.003869973289159534, SE/CI pinned) — verified byte-identical across two runs. Two tests: frozen-
  literal `toBe` pin + same-seed→identical-JSON stability. Reworded a header comment to drop the literal
  `scripts/spikes` token (verify.sh's fs/runner grep false-tripped on it). verify.sh PASS: typecheck 0,
  799 unit tests pass, determinism suite collected. Status→done.

## Verify
**Last verify:** PASS (2026-07-14T14:22:02Z)

## Evaluation
**Last evaluation:** PASS (2026-07-14T14:24:37Z)
