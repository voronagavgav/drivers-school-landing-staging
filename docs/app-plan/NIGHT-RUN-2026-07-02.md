# Night run — 2026-07-02 (Danil asleep ~00:00–07:00, full autonomy)

Danil's ordered backlog for tonight (verbatim intent), executed fully autonomously — reversible defaults,
every decision recorded HERE + PLAN.md for morning veto. This file is the resume-point if any session/context
boundary hits: read it, check "STATE", continue the first unchecked item.

## Ground rules (standing)
- Taste gate: Danil reviews VISUALS in the morning — build + deploy PREVIEWS (landing variants, app UI),
  never flip the live landing's design irreversibly overnight. Pushing to the preview URL = established loop.
- App build work routes through the Mesa harness (bulk=Opus); landing repo (~/pdr-landing-site) is
  hand-applied (no harness scaffold) with render-verify before push. Read
  `~/.claude/skills/pdr-design/landing-perf.md` BEFORE touching landing perf/map/phone.
- Model tiering: Fable = control/judge only; Opus = all bulk. Watch usage limits — moderate fan-outs,
  sequential heavy phases ([[feedback-budget-scope-biggen]], proc ceiling ~40 free).
- Commit+push each completed step (drivers-school + pdr-landing-site → GitHub; Mesa link is DEGRADED tonight
  — hung-up pushes are expected, local commits + Stop-hook retries suffice, do NOT fight it).

## Pipeline (in order; ✅ = done, ▶ = in progress)

### 0. ▶ Finish current task: wave10f (SRS fix wave)
- run-all completes (16/19 at plan time) → independent verify (typecheck · unit incl. reference-vectors ·
  db:seed + integration · build · migrate-diff drift EMPTY) → first `wave-review` run (range db1deac-era
  wave10f base..HEAD, focus default panel) → fix confirmed findings → PLAN.md closure → push.

### 1. Landing audit (code: optimization / device support / smoothness / reasonableness) + apply
- Adversarial audit Workflow (Opus reviewers) over ~/pdr-landing-site (index.html, assets/landing.{css,js},
  assets/map-bg.{css,js}, map-bg-frozen.html): JS perf+correctness, CSS/glass discipline, HTML/a11y/meta,
  device-tier gating vs landing-perf.md rules, hosted-PSI measurement (landing IS public — PSI works).
- I (Fable) judge confirmed findings → apply conservative fixes (NO visual identity changes; ?v bump in
  lockstep only if assets change) → local render check (+e2b only if glass-affecting) → push → re-PSI.

### 2. Full app UX test (all functions/buttons, user POV, desktop + PHONE viewport)
- Serve: `npm run build` (fresh post-wave10f) → `npm run db:seed` →
  `SESSION_SECRET=x COOKIE_SECURE=false npm run start -- -H 0.0.0.0 -p 3100` → origin http://100.110.64.90:3100.
- `npm run audit:browser` (existing 14 assertions) first; then EXPLORATORY UX passes with agent-browser
  (sequential, proc ceiling): (a) desktop full-journey — register→onboarding→dashboard→practice modes→
  exam→mistakes→saved→progress→history→account→admin; every button/link/empty state/error path;
  (b) PHONE viewport (390×844) same journey — layout breaks, tap targets, keyboard, scroll issues.
- Output: bugs-fixed-now (small: inline or a mini harness run) vs FINDINGS+IDEAS doc
  (`docs/app-plan/UX-FINDINGS-2026-07-02.md`): unique features, user life-easings, phone-specific issues —
  these FEED Wave 11/12 specs, not ad-hoc overnight builds.

### 3. Connect application ↔ landing page
- Landing CTAs («Почати безкоштовно» etc.) currently dead-end. Wire them to a SINGLE configurable app
  origin (one const in assets/landing.js or hrefs) + sensible deep links (/register, /login).
- App has NO public origin (Tailscale-only). Overnight default (reversible): attempt a `cloudflared` quick
  tunnel (no account needed) → if stable, use its URL for tonight's phone/PSI testing of the APP and set the
  landing to the PLACEHOLDER pattern (documented const, default = production-TBD). The DURABLE public origin
  (domain/hosting) is a MORNING DECISION for Danil — recorded as the top open question.
- Also align app root page (app/page.tsx) so a landing→app arrival lands sensibly (register/login/dashboard).

### 4. Time-permitting: SEO + conversion + PHONE-FIRST landing UI
- SEO: meta/OG/twitter cards, schema.org (Course/FAQ), sitemap.xml, robots.txt, canonical, lang/hreflang,
  semantic headings — landing repo, low-risk, push.
- Conversion: analyze CTA placement/friction vs the calm-brand rules (no dark patterns — trust IS the moat);
  apply copy/structure tweaks as a PREVIEW variant if visual, direct if invisible.
- PHONE-FIRST landing UI (Danil: "full unique UI shaped exactly for phone; resizing desktop works not good"):
  build `~/pdr-landing/variant-phone.html` (or a `phone.css` layer) as a DEDICATED mobile experience per
  pdr-design taste + landing-perf device tiers; deploy to preview for morning review. START from
  `~/pdr-landing/CURRENT-STATE.md` (phone open-items already listed there). Do NOT flip live routing to it
  without Danil.

### Morning deliverable
- PLAN.md entries per completed item + this file's STATE updated + a compact MORNING-REPORT section at the
  bottom of this file: what shipped, what's in preview for his eye, open decisions (public origin!), costs.

## STATE (update as you go)
- [~] 0 wave10f BUILT (19/19 tasks, 22 ticks, $21.53 on Opus) + my verify GREEN (typecheck 0 · 351 unit/32
      files incl. the ts-fsrs golden vectors · db:seed 2322 · 70 integration/20 files · build 0 · migrate-diff
      drift ZERO · ReviewLog FK RESTRICT live in DB) · both spikes POSITIVE (Serwist WORKS via
      `next build --webpack`; sharp AVIF ~9 KB on Node 25 — W13 fully de-risked) — wave-review running,
      PLAN.md closure after its verdict.
- [x] 1 landing audit DONE+PUSHED (?v=6): 12 raw findings → 6 confirmed by skeptics → 5 applied
      (span3 blur-budget flatten · prefers-contrast JS map gate · nav will-change dropped · ODbL
      attribution added · Google-Fonts render-blocking hop removed via 20 self-hosted woff2 + fonts.css)
      + verified over HTTP desktop+phone, 0 console errors. DEFERRED (taste, morning): glass-emu default
      for Safari/FF desktops — flattens glass on strong Macs too. PSI re-measure blocked (keyless 429).
- [~] 2 DESKTOP pass DONE — found a true BLOCKER (unguarded crypto.randomUUID broke ALL answering over
      plain http; every exam 0/20) → FIXED + browser-audit extended to 16 assertions incl. an actual
      answer-click (16/16 green on the new build). Also fixed: onboarding default A→B, localized 404,
      Состав→Склад. Deferred findings + ideas → `UX-FINDINGS-2026-07-02.md` (feeds W11/W12 specs).
      Wave10f wave-review verdict PROCEED-WITH-CHANGES → all 4 confirmed findings fixed same night
      (three-band deriveGrade — Good(3) was unreachable in prod; first-attempt-only FSRS; queue
      overdueness spec formula; MIN_STABILITY 1e-3). Wave 10f CLOSED in PLAN.md (a23b0cb).
      PHONE UX explorer RUNNING.
- [x] 3 landing→app wiring SHIPPED (9 `data-app` links: /login ×2, /register ×7; `APP_ORIGIN=''` keeps
      anchors — no dead links on the public preview; `?app=<origin>` override verified end-to-end locally).
      cloudflared tunnel DENIED by the permission classifier (outward-facing — correctly Danil's call) →
      public end-to-end + app-PSI fold into MORNING DECISION #1: the app's public origin.
- [x] 4a SEO shipped (cd9de8a): JSON-LD WebSite+FAQPage from the real FAQ (honest data ONLY — no fabricated
      ratings), apple-touch-icon (Світлик 180px), twitter:image:alt.
- [x] 4b conversion — folded into 3 + 4c (deep-link wiring, thumb-zone sticky CTA dock, sample-question-as-
      hero); copy/placement experiments deliberately NOT done unsupervised (taste) — recommendations below.
- [x] 4c phone-first landing variant SHIPPED to preview + taste-checked (PASSES the pdr-design rules):
      **https://voronagavgav.github.io/pdr-landing-preview/phone.html?v=6**
- [x] BONUS: **Wave 11 COMPLETE** — built (16 tasks, $30.76) + independently verified (376 unit ·
      88 integration · build 0 · drift zero · audit **17/17** incl. the new `/admin/readiness-shadow`
      assertion) + wave-reviewed (7 confirmed, 0 rejected — incl. a real major: SPACED_REVIEW wasn't
      due-only) + all fixes landed same night (932b4f5). The adaptive loop, study plan, streaks, nightly
      job, and the dial-calibration shadow view are LIVE server-side. Next wave = W12a (design-system
      port) — the first learner-visible one, i.e. YOUR taste territory.

---

# ☀️ MORNING REPORT (Danil: start here)

## What shipped tonight (all pushed, all verified)
1. **Wave 10f closed** — all 18 review findings fixed + golden-vector oracle + both W13 spikes POSITIVE
   (Serwist works via `next build --webpack`; sharp AVIF ≈9 KB). Then its own wave-review found 4 more
   (headline: production grading could never emit Good(3) — fixed with three-band latency) — fixed same night.
2. **App answering was BROKEN over real HTTP and is fixed** — unguarded `crypto.randomUUID` threw on every
   option click (every exam 0/20). Real-browser UX audit caught it; the browser audit now ANSWERS a question
   as a permanent regression assertion (16/16, now 17 planned with the W11 shadow view).
3. **Landing**: 5 audit fixes + self-hosted fonts (render-blocking Google hop gone) + ODbL attribution +
   SEO structured data → live at **…/pdr-landing-preview/?v=6**.
4. **Landing↔app connected**: 9 `data-app` deep links behind one `APP_ORIGIN` const (anchors until the
   origin exists; `?app=` override tested).
5. **Full UX test, desktop + phone**: verdict "genuinely well-built"; fixed tonight: iOS-zoom inputs,
   nav-fade affordance, onboarding defaults to B, localized 404, Состав→Склад. Everything else routed
   into wave specs: `UX-FINDINGS-2026-07-02.md`.
6. **Phone-first landing variant** (your "full unique UI for phone") — **review on your phone:**
   **https://voronagavgav.github.io/pdr-landing-preview/phone.html?v=6**
7. **Wave 11 building overnight** (server core + admin `/admin/readiness-shadow` — the dial-calibration
   instrument; no learner UI).

## Decisions needed from you (in priority order)
1. **App public origin** (blocks landing↔app going truly live + app PSI + the W13 image cache/push host):
   domain + where the box serves publicly (cloudflared named tunnel? reverse proxy?). The quick-tunnel
   test was permission-blocked (correctly — outward-facing is your call).
2. **Phone landing variant** — approve / iterate (then decide how it routes: UA-redirect vs adaptive serve).
3. **Deferred landing audit finding**: default Safari/FF desktops to emulated glass? (GPU-blind detection;
   would flatten glass on strong Macs too — I declined to ship it unsupervised.)
4. **Pre-production honesty pass** (before any indexing): «Тисячі вже склали», ★4.9, testimonials.

## Conversion recommendations (not applied — taste)
- Hero CTA copy test: «Почати безкоштовно» vs «Спробувати одне питання» (the sample question converts
  curiosity better than a register wall; pairs with a guest/demo mode — currently NO guest mode exists,
  the biggest conversion lever the app itself could add).
- Pricing: «Безкоштовно назавжди» tier framing before Premium (calm-trust positioning), one-line refund
  promise; keep zero dark patterns (trust IS the moat).
- Post-sample-question moment on the landing = THE registration hook: after a correct answer, offer
  «Зберегти прогрес → створити акаунт» instead of a generic CTA.

## Costs (night): wave10f $21.53 · workflows/agents ≈ $25–35 est. · wave11 running (~$20 est.)

## Incident log (lessons, already handled)
- **Driver-branch race:** committed the night plan while the wave10f Serwist spike had the repo checked out
  on `spike/serwist` → commit stranded off-main (looked "deleted"). Recovered by cherry-pick. RULE: while a
  driver run is ACTIVE on a repo, check `git branch --show-current` before any control-thread commit — or
  don't commit to that repo until the run completes.
- **wave-review shakedown:** saved-workflow `name:` invocation served a STALE cached script + args arrived
  as a JSON STRING → hardened the script (accept both) and invoke via `scriptPath` (bypasses the cache).
- **cloudflared quick tunnel** = permission-gated (outward-facing). Don't retry overnight; it's Danil's call.
