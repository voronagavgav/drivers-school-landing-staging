/Users/clpc/mesa/bin/driver.sh: line 194: describe.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: it.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: ctx.skip: command not found
Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
All six acceptance criteria independently verified against the actual artifacts (not just the captured PREVERIFY output):

1. **predict.py pure-Python formula** — reads `retrievability(t,S,w20)` implementing `R=(1+FACTOR·t/S)^(-w20)`, `FACTOR=0.9^(-1/w20)-1`, stdlib `math`/`json`/`sys` only; no `subprocess`/`tsx`/`node` on non-comment lines. Mirrors `lib/fsrs/retrievability.ts` exactly. ✓
2. **Anchors** — ran `--self-check`: `ok anchor elapsed0 R=1 maxabsdiff=0.0` / `ok anchor elapsedS R=0.9 maxabsdiff=0.0`, exit 0, across w20∈{0.1542,0.12,0.20}. ✓
3. **Grid** — fixture has 25 rows; every `elapsedDays==0⇒r==1.0`, every `elapsedDays==stability⇒r==0.9`. ✓
4. **TS re-emit byte-stable** — ran the emitter: 25 rows, keys match, maxabsdiff vs fixture = 0. ✓
5. **Cross-check** — ran `--crosscheck`: `maxabsdiff=4.779055293724355e-07` (< 1e-6), no MISMATCH; PREVERIFY-OUTPUT.txt carries the `static evidence` header. ✓
6. **typecheck** — ran `npm run -s typecheck`, exit 0. ✓

**Structural-trap check passed:** this is a genuine cross-*implementation* oracle, not self-referential. `predict.py` derives values from the FSRS-6 formula; the expected fixture comes from the TS `retrievability` which is externally oracle-verified against py-fsrs 6.3.1 (wave19a). The non-zero cross-check diff (4.78e-7) itself proves the two sides are independent (a copied-from-implementation test would read exactly 0). The two construction anchors (R(0)=1, R(S,S)=0.9) are formula invariants holding for arbitrary w20 — impossible to satisfy by self-reference. No fixture-dodging, no weakened test, no production-bypass.

The prior REJECT was the documented "no VERDICT marker" evaluator glitch, not a substantive gap; the Acceptance table + PREVERIFY artifact now map every criterion to a readable produced file.

VERDICT: ACCEPT
