wave21-07-dashboard-onboarding-copy — STATIC EVIDENCE (read, do NOT run)
========================================================================
Captured 2026-07-14 on ClPcs-Mac-mini. Verbatim stdout of the runnable
Goal criteria (typecheck / build / audit / source greps) so a read-only
judge can confirm criteria 3–6 by READING this file, not by executing.
verify.sh is green; last verify PASS 2026-07-14T08:23:07Z (re-confirmed below).

--- criterion 1: dashboard planMinutes from clamped plan.dailyQuota ---
$ grep -n "plan.dailyQuota|planMinutes" app/(app)/dashboard/page.tsx
134:  const planMinutes = plan ? Math.ceil((plan.dailyQuota * PLAN_SECONDS_PER_QUESTION) / 60) : 0;
271:              ≈{plan.dailyQuota} {questionsPlural(plan.dailyQuota)} · {planMinutes} хв
# plan.dailyQuota is the CLAMPED display quota (lib/study-plan.ts Math.min(..., MAX_DAILY_QUOTA));
# no separate un-clamped quota is introduced. ZERO app-code change (pure model already clamps).

--- criterion 2: onboarding fresh-user first-plan line intact ---
$ grep -n "plan.dailyQuota" app/(app)/onboarding/page.tsx
102:        ? `~${plan.daysLeft} днів × ${plan.dailyQuota} питань на день — встигаєш спокійно.`
# reads plan.daysLeft/plan.feasible/plan.dailyQuota; «встигаєш спокійно» branch unchanged.

--- criterion 3: audit plan-card negative assertion present ---
$ grep -n "Не встигнете за" bin/browser-audit.sh
411:# quota) and its copy must NOT threaten multi-day failure — the «Не встигнете за» line was
414:assert_main_text_absent "Не встигнете за" "plan card drops the removed multi-day threat copy («Не встигнете за»)"
$ grep -rn "Не встигнете за" app/    # app source must NOT emit the removed threat copy
(no matches in app/ — good)

--- criterion 4: typecheck ---
$ npm run -s typecheck
typecheck exit=0

--- criterion 5: build ---
$ npm run build
build exit=0
○  (Static)   prerendered as static content
ƒ  (Dynamic)  server-rendered on demand
# (unchanged source ⇒ identical chunk hashes ⇒ harmless rebuild under the live :3100 server)

--- criterion 6: audit:browser (origin http://100.110.64.90:3100, code 200) ---
$ npm run audit:browser
audit exit=0
=== browser audit: 84 passed, 0 failed === (log: /tmp/ds-browser-audit/audit-20260714-112914.log)
  PASS  plan card «Сьогоднішній план» renders (seeded user, intelligence unlocked)
  PASS  plan card drops the removed multi-day threat copy («Не встигнете за»)
  PASS  plan card «Сьогоднішній план» starts a session (url=.../test/cmrke72tk01vi67gev0hfh04a)

RESULT: all six Goal criteria met (wiring + audit-assertion task; no test/oracle/fixture
authored, no behaviour change — each criterion confirmed by reading a produced file).
