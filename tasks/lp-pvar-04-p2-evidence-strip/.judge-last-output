/Users/clpc/mesa/bin/driver.sh: line 194: describe.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: it.skip: command not found
/Users/clpc/mesa/bin/driver.sh: line 194: ctx.skip: command not found
I independently verified every acceptance criterion against the actual produced state (not the journal's self-report):

**Files & structure**
- `layout.tsx:8` — `robots: { index: false, follow: false }`, returns `{children}` ✓
- `page.tsx:11` — `<V36Body hero={<HeroProspekt />} navDark proofSlot={<P2Proof />} />`; `HeroProspekt` export confirmed present ✓
- `copy.ts:15` — imports `BANK_B_FMT, IMG_FMT, SECTIONS, YEAR` from `../copy`; parent exports all four; no `757` retyped (only in `.ts` doc comments, and criterion 8 scopes the literal-757 ban to `.tsx`) ✓

**Evidence strip**
- 12 distinct `/restyled-live/*.png` sources referenced (≥8), and I confirmed all 12 files physically exist ✓
- `986` anchored to illustrations (`imgCaption`), `1 757` + «Офіційний банк питань 2026» as subordinate prose, no `«ГСЦ МВС»` ✓
- No overclaim tokens (`усі 986`/`всі 986`/`типов`) — grep clean ✓
- `_proof.tsx:40` `aspect-ratio:4/3` fixed box + `:67` `loading="lazy" width={208} height={156}` ✓
- No animation → reduced-motion guard vacuously met ✓

**Runnable evidence (I re-ran, did not trust the artifact)**
- `npm run -s typecheck` → exit 0 ✓
- `git status --porcelain` → only task-dir files; the four `p2/` files are committed and tracked ✓
- **Live browser** verify.sh ran (DRIVER_BROWSER_CMD=`agent-browser` present, server up): fresh output across all three viewports — `bands=1 imgs=12 docOverflow=0 hasAll=1 minBody=6.71` at 390/768/1440. Exactly one `.proof` region (V36Body swaps the default band at `_body.tsx:762`), ≥8 restyled imgs, no doc overflow, caption contrast 6.71 ≥ 4.5 ✓

**Structural-trap check**: no test/oracle/fixture authored (pure IMPL task) → traps (a)–(e) inapplicable by construction. Browser verification exercises the real served route, not an internal-fn bypass. verify.sh is intact/not weakened.

All 12 booleans objectively met, confirmed against reality.

VERDICT: ACCEPT
