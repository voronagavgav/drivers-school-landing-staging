# Engineering roadmap — what's left after Wave 14 (written 2026-07-03)

Status baseline: Wave 10–14 program COMPLETE (PLAN.md 2026-07-03 entry). App is feature-complete and
verified locally: typecheck 0 · 554 unit · 154 integration · drift 0 · audit 52/52 over real transport.
Everything below is the recorded remainder — nothing here is new scope, it's the deferred/parked list
sequenced by dependency. Feature waves run through the standard harness (`mesa plan` → `run-all`,
Fable planner / Opus ticks), then independent verify.

## Gate 0 — the launch decision (PARKED by Danil; blocks most of Wave 15)
Pick the public origin + hosting target for the app (and whether the landing stays on GitHub Pages or
moves under the same domain). This is a product/ops decision, not a build task — but Web Push, the
Postgres trigger, sharp prebuild target, and the prod audit all hang on it (MASTER-PLAN OQ1).

## Wave 15 — launch readiness (unblocked by Gate 0)
1. **Production deploy pipeline** — `next build --webpack` path (Spike 1 verdict), sharp AVIF
   build-time prebake (Spike 2 verdict — no repo dep added yet), env/secrets, health checks.
2. **Postgres cutover** — trigger was "volume + deploy target"; queries are already P2029-safe
   (chunked ≤200 / relation-filter mandate held through W10–14). Prisma migrate + data move + re-run
   the integration suite against Postgres.
3. **Web Push / VAPID** — needs the public origin + an off-box recurring scheduler; iOS = installed-PWA
   16.4+. In-app nudges already shipped (W14) — this is the escalation channel only.
4. **Prod runtime audit** — re-run the 52-point audit over the real HTTPS origin (per
   [[feedback-runtime-audit]] — real browser, real transport), plus hosted PSI on the prod URL.
5. **Landing → prod hygiene** — strip the `?glass/?map/?heat/?qa` diagnostics; real legal pages
   (links are dead); Premium CTA → real signup or hide; political map labels decision
   (`путiн хуйло` / `Свинська область` — brand risk, Danil's call); placeholder social proof
   (★4.9 / testimonials / user counts) → real data or honest copy.

## Wave 16 — deferred product features (post-launch by design, buildable earlier)
1. **QUICK / MARATHON / SIGN_TRAINER modes** — deferred by name in the mode-union decision (C2);
   cheap presets over the existing picker. SIGN_TRAINER pairs with sign-corpus coverage.
2. **Diagnostic onboarding seed** — seed initial ReviewState from the DIAGNOSTIC session instead of
   pure lazy-create.
3. **Per-user FSRS optimizer** — per-user parameter fit once ReviewLog volume justifies it.
4. **Monetization** — decision stands: launch FREE (G3); transparent subscription is a post-launch
   vision item (C4). Not scheduled.

## Landing / design engineering follow-ups (small, independent of Gate 0)
- **Back-port deploy-only changes to canonical** — variant-b/index.html's emu-glass, phone gates,
  frozen-map work live only in `~/pdr-landing-site`; a `_design/` regen would wipe them.
- **Repo history cleanup** — 44MB pmtiles in `pdr-landing-preview` git history (repo 210M) →
  `git filter-repo`.
- **Desktop scrim on section-heads/readiness text** (a11y contrast) — needs Danil's eye.
- **Needs real hardware (Danil):** phone GPU-heat/battery check (phone-opt item #2) ·
  `phone.html` phone-first variant review (committed 2026-07-02, awaiting morning review) ·
  Safari glass investigation (PARKED).

## Content pipeline (parallel track, not app code)
- **Image restyle:** 60/1077 approved · ~51 batches remaining at 20/batch · loop = generate →
  restyle-verify workflow → Danil review → golive. Throughput-limited by human review, not eng.
- Content/data engineering itself (stable-KEY loader, q-image resolver, quality stats) is DONE (W6–9).

## Recommended order
1. Landing small fixes (back-port, filter-repo) — anytime, low risk.
2. **Gate 0 decision** ← the only thing between "done locally" and "launched".
3. Wave 15 as one harness program (deploy → Postgres → push → prod audit → landing hygiene).
4. Wave 16 after real-user signal.
Restyle batches keep running in parallel throughout.
